{
  "name": "block git commit --no-gpg-sig (abbreviated long-option for --no-gpg-sign)",
  "stdin": { "tool_input": { "command": "git commit --no-gpg-sig -m \"x\"" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--no-g"]
}

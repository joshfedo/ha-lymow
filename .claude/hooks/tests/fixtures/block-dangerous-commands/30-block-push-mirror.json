{
  "name": "block git push --mirror (mirrors every ref to remote)",
  "stdin": { "tool_input": { "command": "git push --mirror origin" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--mirror"]
}

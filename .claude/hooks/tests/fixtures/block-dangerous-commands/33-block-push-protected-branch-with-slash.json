{
  "name": "regex-metachar safety: protected name 'release/v1' blocks push to release/v1 (slash is escaped, not treated as regex)",
  "stdin": { "tool_input": { "command": "git push origin release/v1" } },
  "env": { "CLAUDE_PROTECTED_BRANCHES": "main,master,release/v1" },
  "expect_exit": 2
}

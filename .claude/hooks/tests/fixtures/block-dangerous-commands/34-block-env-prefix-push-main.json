{
  "name": "block env FOO=1 git push origin main (env-var prefix shouldn't bypass)",
  "stdin": { "tool_input": { "command": "env FOO=1 git push origin main" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["main"]
}

{
  "name": "block git push --force-with-lease origin main (long flag before remote)",
  "stdin": { "tool_input": { "command": "git push --force-with-lease origin main" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["deny", "main"]
}

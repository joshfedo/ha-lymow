{
  "name": "block DELETE FROM where the only WHERE is inside a /* */ block comment",
  "stdin": { "tool_input": { "command": "psql -c \"DELETE FROM users /* WHERE id = 1 */\"" } },
  "expect_exit": 2
}

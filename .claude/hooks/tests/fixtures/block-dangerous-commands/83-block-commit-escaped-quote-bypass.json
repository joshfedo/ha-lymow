{
  "name": "block git commit -m \"fix \\\" & note\" --no-verify (escaped quote inside message before separator)",
  "stdin": { "tool_input": { "command": "git commit -m \"fix \\\" & note\" --no-verify" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--no-verify"]
}

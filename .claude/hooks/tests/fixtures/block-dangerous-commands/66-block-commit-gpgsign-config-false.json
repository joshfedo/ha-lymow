{
  "name": "block git -c commit.gpgsign=false commit (git-level -c BEFORE subcommand — the actual signing-bypass form)",
  "stdin": { "tool_input": { "command": "git -c commit.gpgsign=false commit -m \"x\"" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["commit.gpgsign"]
}

{
  "name": "block git commit --no-verify (skips pre-commit hooks per CLAUDE.md)",
  "stdin": { "tool_input": { "command": "git commit -m \"x\" --no-verify" } },
  "expect_exit": 2,
  "expect_stdout_contains": ["--no-verify"]
}

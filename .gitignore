# Secrets and captured traffic — never commit these
*.pcap
*.pcapng
*.har
*.txt
secrets.yaml
.env
capture-*.txt

# APK analysis binaries (large, not for VCS)
tools/apk/

# Generated script outputs
scripts/map.png
scripts/map_response.bin

# Capture output files (may contain device IDs / PII)
tools/*.jsonl
tools/*.bin

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Editor
.vscode/
*.swp

# Local AI session task tracking
.todo
plan.md
.coverage

# dotclaude personal / runtime files (never commit)
.claude/settings.local.json
.claude/scheduled_tasks.lock
CLAUDE.local.md
